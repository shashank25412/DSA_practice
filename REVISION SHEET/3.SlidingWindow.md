# Sliding Window

### Core Philosophy

Sliding Window = maintain a **contiguous range** `[L...R]` while reusing previous work.

Pattern:

```text
Expand → Evaluate → Fix (if invalid) → Continue
```

- **Right Pointer (R)** → EXPAND (gain information)
- **Left Pointer (L)** → REPAIR (remove information)

---

### Why Sliding Window is O(n)

Each element:

- enters the window once
- leaves the window once

Pointers only move forward.

```text
O(2N) = O(N)
```

---

### Fixed Window Pattern

Use when:
>window size = k

Template:

```cpp
expand

if(size > k)
    shrink

if(size == k)
    update answer
```

### Examples

| Problem | Validity | Core State |
|---|---|---|
| Maximum Average Subarray I | `windowSize == k` | Rolling Sum |
| Maximum Number of Vowels | `windowSize == k` | Vowel Count |
| Find All Anagrams | `windowSize == patternSize` | Frequency Map |
| Permutation in String | `windowSize == patternSize` | Frequency Match |

---

### Dynamic Window Pattern

Use when:
>window size is unknown

Template:

```cpp
expand

while(invalid)
    shrink

update answer
```

#### Key Mental Model

```text
Expand aggressively
Shrink only when forced
```

---

### Dynamic Window Fundamentals

#### LC 643 — Maximum Average Subarray I

| Topic        | Meaning            |
| ------------ | ------------------ |
| Pattern      | Fixed Window       |
| Goal         | Max average        |
| Validity     | `windowSize == k`  |
| Expand       | Add new number     |
| Shrink       | Remove left number |
| Answer Logic | Max sum / avg      |
| Intuition    | Rolling sum        |

---

#### LC 1456 — Maximum Number of Vowels

| Topic        | Meaning                   |
| ------------ | ------------------------- |
| Pattern      | Fixed Window              |
| Goal         | Max vowels                |
| Validity     | `windowSize == k`         |
| Expand       | Add vowel contribution    |
| Shrink       | Remove vowel contribution |
| Answer Logic | Max count                 |
| Intuition    | Gain/Loss                 |

---

#### LC 1004 — Max Consecutive Ones III

| Topic        | Meaning                |
| ------------ | ---------------------- |
| Pattern      | Dynamic                |
| Goal         | Longest valid subarray |
| Validity     | `zeroCount <= k`       |
| Invalid      | `zeroCount > k`        |
| Expand       | Include element        |
| Shrink       | Remove until valid     |
| Answer Logic | Max length             |
| Intuition    | Flip ≤ k zeros         |

---

#### LC 3 — Longest Substring Without Repeating Characters

| Topic        | Meaning                  |
| ------------ | ------------------------ |
| Pattern      | Dynamic                  |
| Goal         | Longest unique substring |
| Validity     | No duplicates            |
| Invalid      | `freq[s[r]] > 1`         |
| Expand       | Add frequency            |
| Shrink       | Remove duplicate         |
| Answer Logic | Max length               |
| Intuition    | Unique substring         |

---

#### LC 904 — Fruit Into Baskets

| Topic        | Meaning            |
| ------------ | ------------------ |
| Pattern      | Dynamic            |
| Goal         | Longest valid      |
| Validity     | `distinct <= 2`    |
| Invalid      | `distinct > 2`     |
| Expand       | Add fruit          |
| Shrink       | Remove until valid |
| Answer Logic | Max length         |
| Intuition    | At most K distinct |


</br>

---

### High-Value Sliding Window Problems


| LC # | Problem                                        | Pattern       | Validity Condition        | Expansion Rule | Shrink Rule       | Answer Logic  | Core Intuition     |
| ---: | ---------------------------------------------- | ------------- | ------------------------- | -------------- | ----------------- | ------------- | ------------------ |
|  643 | Maximum Average Subarray I                     | Fixed         | `size == k`               | Add sum        | Remove left       | Max Avg       | Rolling sum        |
| 1456 | Maximum Number of Vowels                       | Fixed         | `size == k`               | Add vowel      | Remove vowel      | Max Count     | Gain/Loss          |
| 1004 | Max Consecutive Ones III                       | Dynamic       | `zeros <= k`              | Expand         | Shrink if invalid | Max Length    | Flip zeros         |
|    3 | Longest Substring Without Repeating Characters | Dynamic       | No duplicates             | Add freq       | Remove duplicate  | Max Length    | Unique substring   |
|  904 | Fruit Into Baskets                             | Dynamic       | `distinct <= 2`           | Add fruit      | Shrink            | Max Length    | At-most-K distinct |
|  424 | Longest Repeating Character Replacement        | Dynamic       | `(windowSize-maxFreq)<=k` | Add freq       | Shrink            | Max Length    | Replace chars      |
|  567 | Permutation in String                          | Fixed + Freq  | Pattern size              | Add freq       | Remove freq       | Exact match   | Anagram            |
|  438 | Find All Anagrams                              | Fixed + Freq  | Pattern size              | Add char       | Remove char       | Store indices | Sliding histogram  |
|  209 | Minimum Size Subarray Sum                      | Dynamic       | `sum >= target`           | Expand         | Shrink valid      | Min Length    | Smallest valid     |
|   76 | Minimum Window Substring                       | Dynamic       | All chars covered         | Add freq       | Shrink valid      | Min Length    | Covering window    |
|  713 | Subarray Product Less Than K                   | Dynamic       | `product < k`             | Multiply       | Divide            | Count         | Multiplicative     |
|  930 | Binary Subarrays With Sum                      | Prefix + SW   | Exact sum                 | Expand         | Careful shrink    | Count         | AtMost trick       |
| 1493 | Longest Subarray of 1s After Deleting One      | Dynamic       | `zeros <= 1`              | Expand         | Shrink            | Max Length    | Delete one         |
| 2401 | Longest Nice Subarray                          | Bitmask       | No overlapping bits       | OR value       | Remove            | Max Length    | Bitwise            |
| 1838 | Frequency of Most Frequent Element             | Sort + Window | Cost <= k                 | Expand         | Shrink            | Max Length    | Equalization       |

---

### Sliding Window Categories

#### 1. Fixed Window
Known size `k`

#### 2. Longest Valid Window
Pattern:

```cpp
while(invalid)
    shrink
```

#### 3. Minimum Valid Window
Pattern:

```cpp
while(valid)
{
    minimize answer
    shrink
}
```

#### 4. At Most K Constraint

Examples:

```text
zeros <= k
distinct <= k
replacements <= k
```

#### 5. Frequency Matching

Maintain:

```text
frequency map
```

Used in:

- Anagrams
- Permutations
- Minimum Window Substring

#### 6. Counting Windows

Often uses:

```text
Exactly(K) = AtMost(K) - AtMost(K-1)
```

---

### Interview Recognition Logic

#### Q1. Is it contiguous?

Keywords:

```text
subarray
substring
continuous
```

If yes → Sliding Window candidate

#### Q2. Fixed or Dynamic?

Known size?

- YES → Fixed Window
- NO → Dynamic Window

#### Q3. What makes window invalid?

Examples:

```text
duplicates
too many zeros
too many distinct chars
sum exceeded
missing chars
```

This defines:

```cpp
while(invalid)
    shrink
```

#### Q4. Longest or Minimum?

Longest:

```cpp
answer = max(...)
```

Minimum:

```cpp
while(valid)
{
    answer=min(...)
    shrink
}
```

---

### 5–10 Minute Revision Sheet

#### Pointer Roles

**Right Pointer**
>EXPAND

**Left Pointer**
>REPAIR


---

### Golden Question

> What makes the window invalid?

Answer this and the pattern usually becomes obvious.

---

### Master Templates

#### Fixed Window

```cpp
for(int r = 0; r < n; r++) {
    expand();

    if(windowSize > k)
        shrink();

    if(windowSize == k)
        updateAnswer();
}
```

#### Dynamic Window

```cpp
for(int r = 0; r < n; r++) {
    expand();

    while(invalid())
        shrink();

    updateAnswer();
}
```